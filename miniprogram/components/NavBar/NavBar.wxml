<view class="nav" style="{{navStyle}}">
  <!-- 导航栏标题 -->
  <view class="title" style="{{titleStyle}}">{{ title }}</view>
  <!-- 胶囊按钮 -->
  <view class="menu-btn" wx:if="{{showIcon}}" style="{{menuStyle}}">
    <view class="btn-wrapper" style="{{menuBtnStyle}}">
      <view class="icon icon-back" bindtap="stepBack"></view>
      <view class="icon icon-home" bindtap="returnHome"></view>
    </view>
  </view>
</view>